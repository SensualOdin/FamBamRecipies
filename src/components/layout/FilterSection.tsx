import React from 'react';
import {SlidersHorizontal,Heart} from 'lucide-react';
import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';
export default function FilterSection(p:any){return <div className="binder-filters"><div className="filter-line"><div className="category-scroll"><div className="category-tabs" role="group" aria-label="Recipe categories">{p.categories.map((c:string)=><button key={c} aria-pressed={p.selectedCategory===c} onClick={()=>p.setSelectedCategory(c)}>{c==='All'?'Everything':c}</button>)}</div></div><button className="filter-toggle" aria-expanded={p.showFilters} aria-controls="expanded-filters" onClick={()=>p.setShowFilters(!p.showFilters)}><SlidersHorizontal size={15}/> Refine</button></div>{p.showFilters && <div id="expanded-filters" className="expanded-filters"><div><label htmlFor="sort-recipes">Sort recipes</label><Select value={p.sortBy} onValueChange={p.setSortBy}><SelectTrigger id="sort-recipes"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="newest">Newest first</SelectItem><SelectItem value="popular">Most cooked</SelectItem><SelectItem value="name">A to Z</SelectItem></SelectContent></Select></div><div><label>Difficulty</label><div className="filter-options">{['All','Easy','Medium','Hard'].map(d=><button key={d} aria-pressed={p.selectedDifficulty===d} onClick={()=>p.setSelectedDifficulty(d)}>{d}</button>)}</div></div><div><label>Your selection</label><div className="filter-options"><button aria-pressed={p.showFavoritesOnly} onClick={()=>p.setShowFavoritesOnly(!p.showFavoritesOnly)}>Saved recipes</button><button onClick={p.onResetFilters}>Reset all</button></div></div></div>}</div>}
